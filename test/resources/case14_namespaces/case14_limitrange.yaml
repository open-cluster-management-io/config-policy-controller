apiVersion: v1
kind: LimitRange
metadata:
  name: container-mem-limit-range
spec:
  limits:
    - default:
        ephemeral-storage: '0'
      defaultRequest:
        ephemeral-storage: '0'
      type: Container